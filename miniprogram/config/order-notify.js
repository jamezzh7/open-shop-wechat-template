// Fill this with the template ID from WeChat Mini Program Admin:
// 功能 > 订阅消息 > 选用模板. Recommended fields:
// 订单编号(character_string1), 订单金额(amount2), 订单状态(phrase3),
// 支付时间(time4), 商品信息(thing5).
const ORDER_NOTIFY_TEMPLATE_ID = '';

module.exports = {
  ORDER_NOTIFY_TEMPLATE_ID,
};
